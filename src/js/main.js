// Mobile hamburger header

function toggleMobileMenu(menu) {
    menu.classList.toggle('open');
};

// Button for DDLC in experience section 
function soon(){
    var name = document.getElementById("name");
    var displayName = 'Link will be soon';
    alert(displayName);
};